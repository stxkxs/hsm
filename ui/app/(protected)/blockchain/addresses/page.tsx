import { ComingSoon } from "@/components/coming-soon";

export default function BlockchainAddressesPage() {
  return (
    <ComingSoon
      title="Addresses"
      description="Multi-chain addresses"
      detail="Multi-chain address derivation is not yet wired to the HSM backend. This section will be enabled once the blockchain REST routes land."
    />
  );
}
