# Rust
target/
**/*.rs.bk
*.pdb

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Test data
test-data/*.key
test-data/*.enc
test-data/keys/

# Logs
*.log

# Runtime audit-log output (generated by tests / running server)
audit_logs/

# Environment
.env
.env.local

# Build artifacts
dist/
build/
.claude/worktrees/

# Node (UI + SDKs)
node_modules/
.next/

# sdks/rust is a published library: per Rust convention it does not commit a
# Cargo.lock, so downstream consumers resolve their own. Its CI job deliberately
# omits --locked to match.
sdks/rust/Cargo.lock
